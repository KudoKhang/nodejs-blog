var tacpham = [{
   name : 'songhuong',
   count : 1
},{
   name : 'songhong',
   count : 1
}];

for (llement = aar i = 1; i <= 100000, i++) {
   tacpham[Math.floor(Math.random() * tacpham.length)].count += 1;
};

var tp.forEach({tp ; i} => {
   if (tp.count > tacpham(max).count) max = 1;
})

console.log(tacpham);
console.log('Tac pham ' + tacpham[max].name + 'Nhieu luot ramdom nhat voi ' + tacpham[max].count);

